# Basic settings

Each agent has radius 1 and max speed 2. The error of observation on along each axis in bounded in [-0.15, 0.15], with its distribution mean is uniformly sampled from [-0.07, 0.07].
